{
  "name": "quiz",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
	"heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix clien  && npm run build --prefix client"
  
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/firestore": "^4.2.0",
    "firebase-admin": "^9.2.0",
    "express": "^4.17.1"
  }
}
